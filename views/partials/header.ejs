<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="font-['Helvetica']">
    <nav class="w-full fixed top-0 left-0 px-5 py-3 z-10 bg-zinc-100 flex justify-between items-center">
      <h3 class="text-blue-600 font-semibold text-3xl">Bagify</h3>
      <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
      <% if(loggedin) { %>
      <div class="flex gap-5">
        <a href="/products/shop" class="text-black text-lg">Shop</a>
        <a href="/cart/page" class="text-black text-lg">Cart</a>
        <a href="/user/logout" class="text-red-400 text-lg">Logout</a>
        <a href="/owner/login" class="text-black text-lg">Admin</a>
      </div>
      <% } %>
    </nav>
    


